<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="batch">
	<select id="getCycleList" resultType="cycleVO">
		select * from cycle
	</select>	
	
	<insert id="mergeDaily" parameterType="dailyVO">
		merge into daily using dual on (
											daily.cid = #{cid} and
											daily.pid = #{pid} and
											daily.dt = #{dt}
									   )
		when matched then update set cnt = #{cnt}
		when not matched then insert (cid, pid, dt, cnt) values (#{cid}, #{pid}, #{dt}, #{cnt})
	</insert>
</mapper>